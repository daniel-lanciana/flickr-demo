<h1>Photo#search</h1>
<p>Find me in app/views/photo/search.html.erb</p>

<%= render 'form' %>

<div class="thumbs">
<% @results.each do |p| %>
    <a href="<%= p.url_c %>">
    <%= image_tag p.url_q %>
    </a>
    <br />
<% end %>
</div>